import 'package:flutter/material.dart';

class AuthUserView extends StatelessWidget {
  const AuthUserView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
